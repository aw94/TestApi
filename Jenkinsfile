pipeline {
    agent { dockerfile {
        filename 'Dockerfile'
        label 

        
    } }
    stages {
        stage('Test') {
            steps {
                sh 'ls -al'
            }
        }
    }
}